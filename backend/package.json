{
  "name": "hillel-library-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend server for the Hillel School Library management system (auth + Firestore access + API for the frontend).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
